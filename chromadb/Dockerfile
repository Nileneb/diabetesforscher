# Dockerfile für chromadb Service

FROM chromadb/chroma:latest

# Optional: Füge benutzerdefinierte Konfigurationen oder Abhängigkeiten hinzu
# COPY custom_config.yml /path/to/config/

CMD ["chromadb-server"]